const defaultState = []

//Action = type y payload
function reducer(state = defaultState, { type, payload }){
    switch(type){
        default:
            return state
    }
}

export default reducer